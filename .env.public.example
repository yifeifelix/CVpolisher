# Copy to .env.public on the VPS and replace the placeholders.

# Username for Caddy Basic Auth.
BASIC_AUTH_USER=admin

# Generate with:
# docker run --rm caddy:2.9-alpine caddy hash-password --plaintext 'change-this-password'
BASIC_AUTH_PASSWORD_HASH=REPLACE_WITH_CADDY_HASH
